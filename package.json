{
  "name": "cashsplitter",
  "version": "1.0.0",
  "description": "Mobile-first PWA for splitting expenses among groups",
  "private": true,
  "scripts": {
    "test": "node --test tests/unit/*.test.mjs && npx playwright test --config=tests/e2e/playwright.config.js --reporter=list",
    "test:ui": "npx playwright test --config=tests/e2e/playwright.config.js --ui",
    "test:unit": "node --test tests/unit/*.test.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.52.0"
  }
}
